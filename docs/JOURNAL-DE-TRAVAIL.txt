========================================
JOURNAL DE TRAVAIL - FABLE
========================================
Ce fichier consigne TOUT ce qui a été fait, dans l'ordre.
À mettre à jour à chaque session de travail.
Date de création : 03/09/2026

================================================================
SESSION 1 - 03/09/2026 : Analyse stratégique complète
================================================================
FAIT :
1. Création du projet et du dossier D:\Fable
2. Création de CONTEXTE.txt (vision globale)
3. Arborescence d'analyse stratégique en fichiers txt :
   - 1-etude-marche : PESTEL, étude de marché, analyse
     concurrentielle, clientèle cible
   - 2-analyse-strategique : SWOT, 5 forces de Porter,
     business-model, proposition de valeur
   - 3-strategie : positionnement, risques, roadmap
4. Remplissage complet avec données réelles (recherches web) :
   - Marché interactive fiction : 6,4 Mds$ (2025) -> 14,8 Mds$
     (2034), CAGR 9,8%
   - Storybooks IA : 3,2 Mds$ -> 18,7 Mds$, CAGR 21,8%
   - Concurrence : AI Dungeon, Dorian, NovelAI, Character.AI
   - Personas : Nathan (34, nostalgique), Léa (24, créative),
     Théo (16, joueur mobile)
5. Choix du fournisseur IA (choix-fournisseur-ia.txt) :
   - Benchmark lechmazur/writing : Claude domine la fiction
   - Décision : multi-niveaux
     * Chapitres courants : GPT-5.6 Luna (0,20/1,20 $, ~0,001 $/chapitre)
     * Qualité (fins) : Claude Sonnet 5 (2/10 $)
     * Haute qualité : Claude Opus 5 / Fable 5
     * Fallback : Gemini Flash-Lite, Mistral (RGPD)
6. Économie de l'app (economie-app.txt) :
   - Modèle 3 niveaux : essai gratuit (5 chapitres) + Fable+
     (4,99 €/mois, texte illimité) + crédits (images)
   - 1 image = 3 crédits, packs 0,99 à 9,99 €
   - Coût image : ~0,02 $ (FLUX.1 Dev via fal.ai)
   - Point mort : ~60-80 MAU
   - Marge image ~82 %, Fable+ ~76 %

================================================================
SESSION 2 - 03/09/2026 : Fonctionnalités & structure roman
================================================================
FAIT :
1. Fonctionnalités validées (4-produit/fonctionnalites.txt) :
   - Pur narratif, comptes dès v1, social complet, multimédia,
     personnalisation
   - Décision clé : structure = VRAI ROMAN (pas gamebook)
2. Étude poussée des structures narratives (5-ecriture-narrative/
   structure-romans-tous-genres.txt) :
   - 3 actes, pyramide de Freytag, voyage du héros (12 étapes),
     cercle narratif, 7 points
   - Structure du chapitre (1500/3000/5000 mots)
   - Codes de tous les genres (fantasy, SF, polar, horreur,
     historique, romance + autres)
   - Techniques narratives, anti-AI-slop
3. Étude des chefs-d'oeuvre littéraires (5-ecriture-narrative/
   chefs-doeuvre-litteraires.txt) :
   - 7 mécanismes de génie (thèmes, personnages, prose, etc.)
   - Références par genre (Tolkien, Dune, Christie, Poe, Austen...)
   - 10 leçons transmissibles à l'IA

================================================================
SESSION 3 - 03/09/2026 : Checklist, décisions produit & légal
================================================================
FAIT :
1. Checklist maîtresse (6-avant-le-code/checklist-maitresse.txt) :
   - 10 domaines à penser avant le code
2. Décisions produit (6-avant-le-code/decisions-produit.txt) :
   - Tous les genres au MVP + sous-genres optionnels
   - Nom du héros libre, trait de personnalité optionnel
   - Difficulté au choix, chapitres 1500/3000/5000
   - Styles narratifs : maximum possible
   - 2-4 choix, fins multiples 2-5, prologue systématique,
     couverture après le prologue
   - GESTION DES ÂGES (clé) : 4 tranches
     (-10 / 10-15 / 16-18 / 18+)
   - Contenus réservés 18+ : horreur intense, dark fantasy,
     dark romance, thriller violent, romance explicite
   - Échec adapté à l'âge (souple -> tragique)
3. Conformité & classification (6-avant-le-code/conformite-age.txt) :
   - Classification store : 12+ au MVP, 17+ à l'UGC (v1.2)
   - Âge minimum : 10 ans
   - RGPD France : consentement parental < 15 ans
   - COPPA : < 13 ans aux US
   - Changement d'âge : confirmation vers le haut

================================================================
SESSION 4 - 03/09/2026 : Prompts IA & architecture technique
================================================================
FAIT :
1. Prompts IA définis (6-avant-le-code/prompts-ia.txt) :
   - 5 prompts : story bible, prologue, chapitre, fin, résumé
   - + prompt image, règles anti-AI-slop, modèle par prompt
   - Format JSON pour chaque prompt
2. Architecture technique (6-avant-le-code/architecture-technique.txt) :
   - Stack : Expo + TypeScript + Expo Router + Zustand + Supabase
   - Monorepo : apps/mobile + api + packages/shared + prompts + docs
   - Endpoints API : create, chapter, end, image, credits,
     subscribe, user, health
   - Schéma Supabase : 9 tables (profiles, games, chapters,
     credits_transactions, purchases + V1/V1.2)
   - Sécurité : RLS, clés serveur, quotas, rate limiting
3. Design & UX (6-avant-le-code/design-ux.txt) :
   - 15 écrans spécifiés (splash, âge, auth, accueil, genres,
     héros, paramètres, génération, lecture, résumé, paywall,
     fin, boutique, profil, réglages)
   - Thème : ambre/or + bleu nuit

================================================================
SESSION 5 - 03/09/2026 : DÉBUT DU CODE
================================================================
FAIT :
1. Structure monorepo créée :
   - docs/ (toute la documentation déplacée ici)
   - apps/, api/, packages/shared/, prompts/
2. Git initialisé (git init)
3. Fichiers racine :
   - package.json (workspaces : apps/*, packages/*, api)
   - .gitignore (jamais de .env, node_modules, .expo)
   - .env.example (toutes les clés requises documentées)
   - README.md (structure + démarrage rapide)
4. Projet Expo créé (apps/mobile) :
   - Template blank-typescript (Expo 57, React 19.2, RN 0.86)
   - Package renommé @fable/mobile + script typecheck
   - Support web ajouté (react-dom, react-native-web)
   - Typecheck : OK
5. Package partagé (packages/shared) :
   - src/types.ts : types métier (âges, genres, personnalisation,
     story bible, chapitres, crédits, Fable+, API)
   - Typecheck : OK
6. Squelette API (api/) :
   - lib/llm/provider.ts : couche d'abstraction LLM
     * Interface LLMProvider (anthropic, openai)
     * Router (choix fournisseur selon type de prompt)
     * Mapping des modèles (Sonnet 5, GPT-5.6 Luna)
   - lib/db.ts : client Supabase (service role, côté serveur)
   - api/health.ts : endpoint de test (VercelRequest/Response)
   - vercel.json : config fonctions Vercel
   - Typecheck : OK
7. Test de démarrage Expo :
   - Metro bundler démarre, HTTP 200 sur localhost:8081
   - NB : Node 20.10.0 < requis 20.19.4 (avertissement, OK)

ÉTAT FINAL SESSION 5 :
- Le monorepo compile et typecheck intégralement (mobile +
  shared + api)
- Le serveur Expo démarre
- Structure prête à accueillir les écrans et l'API réelle

================================================================
SESSION 6 - 03/09/2026 : PROTOTYPE FONCTIONNEL DES ÉCRANS
================================================================
FAIT :
1. Expo Router configuré dans apps/mobile :
   - Dépendances : expo-router, react-native-safe-area-context,
     react-native-screens, expo-linking, expo-constants
   - Entrée : main = "expo-router/entry"
   - app.json : nom "Fable", slug "fable", scheme "fable",
     userInterfaceStyle "dark", newArchEnabled
2. Prototype fonctionnel (sans design, données mock) :
   - src/data/mock.ts : genres (6), sous-genres, traits,
     styles, longueurs, difficultés, tranches d'âge
   - src/services/mockStory.ts : générateur de chapitres local
     (simule l'IA : intros/choix/fins par genre, streaming)
   - src/state/store.ts : Zustand + persist (âge, partie en cours)
   - Écrans (Expo Router) :
     * app/_layout.tsx : stack racine
     * app/index.tsx : splash + redirection (âge ou accueil)
     * app/age.tsx : sélection de l'âge (4 tranches)
     * app/home.tsx : accueil (continuer / nouvelle partie)
     * app/new-game.tsx : 3 étapes (genre -> héros -> params)
     * app/game/[gameId].tsx : lecture (chapitres + choix,
       streaming simulé "machine à écrire")
     * app/game/end.tsx : fin du roman
3. Alias de chemins : "@/*" -> src/* dans tsconfig
   (+ "@fable/shared" -> packages/shared)
4. Corrections :
   - Apostrophe non échappée dans mockStory (ligne 182)
   - Chemin d'import mockStory -> ../data/mock
   - Accolade en trop dans app.json
   - baseUrl déprécié en TS6 -> ignoreDeprecations: "6.0"
5. MISE À JOUR NODE :
   - Node 20.10.0 trop ancien pour Expo 57 (requis >= 20.19.4)
   - Installé Node.js 24.19.0 LTS via winget
     (OpenJS.NodeJS.LTS) -> npm 11.17.0
6. VÉRIFICATIONS :
   - Typecheck mobile : OK
   - Export web : bundle 786 modules, aucune erreur
   - Export android : bundle 2.7MB, aucune erreur
   - Serveur Expo : HTTP 200 sur localhost:8082
7. NOTES UTILISATEUR :
   - Le serveur doit être lancé par l'utilisateur dans SON
     terminal pour voir le QR code : cd /d D:\Fable\apps\mobile
     && npm start (le cd /d est obligatoire en cmd pour
     changer de lecteur)
   - Expo Go installé sur le téléphone de l'utilisateur
   - Le prototype couvre : âge -> accueil -> nouvelle partie ->
     lecture (streaming + choix) -> fin

ÉTAT : prototype fonctionnel navigable, tourne en web ET en
natif (Android compilé). À brancher sur l'API réelle ensuite.

================================================================
SESSION 7 - 03/09/2026 : GITHUB + VERCEL
================================================================
FAIT :
1. Comptes vérifiés :
   - GitHub : HyddenTroll (gh auth OK)
   - Vercel : hyddentroll (vercel whoami OK)
2. En attente :
   [ ] Push initial sur GitHub (repo : fable ? public/privé ?)
   [ ] Déploiement Vercel (repo + config)

================================================================
SESSION 8 - 03/09/2026 : STRATÉGIE DE DEV (perf/ergo/léger)
================================================================
FAIT :
1. Stratégie de dev documentée (docs/6-avant-le-code/
   strategie-dev.txt) - 3 piliers :
   - PERFORMANCE : New Architecture, Hermes, re-rendus évités,
     FlatList, animations Reanimated, streaming, mesures
   - ERGONOMIE : onglets bas (pouce), lecture confortable,
     accessibilité (44px, contraste AA), retour tactile
   - LÉGÈRETÉ : bundle minimal, code-splitting Expo Router,
     politique stricte des dépendances, images WebP
   - Politique de librairies : à ajouter seulement au besoin,
     à éviter (Redux, styled-components, libs UI monolithiques)
2. Application au prototype :
   - src/theme/index.ts : thème central (couleurs, espacements,
     radii, typo, touchTarget) - fini les couleurs en dur
   - src/components/Button.tsx : bouton réutilisable avec
     retour tactile (pressed) + accessibilité
   - home.tsx : thème + Button
   - new-game.tsx : thème + FlatList (genres) + Button +
     stocke gameParams dans le store
   - game/[gameId].tsx : thème + feedback tactile sur les choix +
     utilise les VRAIS params utilisateur (genre, style, nb
     choix) au lieu du hack précédent
   - store.ts : ajout de gameParams (persisté)
3. Vérifications :
   - Typecheck mobile : OK
   - Export web : bundle 1.2MB, aucune erreur
4. Commit + push : "Stratégie de dev (perf/ergo/léger) + thème
   central, bouton tactile, FlatList, params dans le store"

================================================================
SESSION 9 - 03/09/2026 : TOUS LES ÉCRANS + STRUCTURE APP
================================================================
FAIT :
1. Explication de la structure d'une app (Expo Router) :
   - 1 fichier dans app/ = 1 écran
   - Le système de fichiers définit la navigation
   - [gameId] = route dynamique, _layout.tsx = cadre commun
2. Navigation restructurée avec ONGLETS :
   - app/_layout.tsx : Stack racine (âge, auth, (tabs),
     new-game, game, paywall, shop, settings)
   - app/(tabs)/_layout.tsx : onglets bas (Accueil + Profil)
   - app/(tabs)/index.tsx : Accueil (continuer / nouvelle
     partie, badge crédits, lien connexion)
   - app/(tabs)/profile.tsx : Profil (compte, stats, succès
     placeholder, Fable+, déconnexion)
3. Nouveaux écrans :
   - app/auth.tsx : connexion/inscription (mock)
   - app/paywall.tsx : Fable+ (4,99 €/mois, 7 jours offerts)
   - app/shop.tsx : boutique crédits (packs depuis @fable/shared)
   - app/settings.tsx : réglages (son, lecture, à propos)
4. Flux onboarding complet :
   index -> âge -> auth -> (tabs) accueil
5. PAYWALL FONCTIONNEL : après le 5e chapitre gratuit, un
   non-abonné est redirigé vers l'écran Fable+ (blocage ch.6).
6. Store enrichi : email, isPremium, credits (mock), addCredits,
   spendCredits.
7. Fin améliorée : stats de partie + boutons.
8. Vérifications : typecheck OK, bundle web 1.2MB OK, serveur
   HTTP 200 (test, puis arrêté - l'utilisateur lance lui-même).
9. Commit + push : "Tous les écrans : onglets accueil/profil,
   auth, paywall, boutique, réglages + blocage 5 chapitres
   gratuits" (648018c)
10. PRÉCISION URL (confirmé avec l'utilisateur) :
   - npm start -> http://localhost:8081/ (port par défaut)
   - Si le port 8081 est occupé, Expo propose un autre port
     (ex : 8082) -> l'URL exacte est affichée dans le terminal
   - Téléphone : QR code Expo Go (même Wi-Fi ou mode tunnel)
11. Le serveur n'est PAS lancé par moi : l'utilisateur le lance
   lui-même (cd /d D:\Fable\apps\mobile && npm start)

SESSION 9 bis - 03/09/2026 : FIX DÉPLOIEMENT VERCEL
================================================================
FAIT :
1. Problème : après le push des écrans, l'URL finale renvoyait
   404. Cause : le build Vercel depuis apps/mobile ne trouvait
   pas @fable/shared (workspace du monorepo) car Vercel
   installait isolément dans apps/mobile.
2. Solution : vercel.json DÉPLACÉ à la RACINE du monorepo :
   - buildCommand : cd apps/mobile && npx expo export --platform web
   - outputDirectory : apps/mobile/dist
   - installCommand : npm install (installe les workspaces,
     donc @fable/shared est résolu)
3. vercel.json supprimé de apps/mobile (conflit possible).
4. Build local validé depuis la racine (export web OK).
5. Redéploiement : https://fable-5pt9sqan8-... -> aliased
   https://fable-app-three.vercel.app -> HTTP 200.
6. Commit + push : 4f98944 "Fix déploiement Vercel".

URLS FINALES :
- Site : https://fable-app-three.vercel.app (HTTP 200)
- Repo : https://github.com/HyddenTroll/fable-app
- Local : http://localhost:8081 (npm start dans apps/mobile)

===============================================================
SESSION 10 - 04/09/2026 : AUTH RÉELLE SUPABASE (fin du mock)
===============================================================
CONTEXTE : l'auth était un prototype (aucun backend, email stocké
en local). On passe à de VRAIS comptes : Supabase (Auth + Postgres
+ RLS), email/password + Google (OAuth), quotas côté serveur.

FAIT :
1. Création du projet Supabase "fable" (ref agnaafnujcwswstcwfuw,
   région eu-west-1) + liaison GitHub.
2. Clés récupérées et stockées dans .env (racine) + apps/mobile/.env
   (variables EXPO_PUBLIC_ pour le client). JAMAIS commitées.
3. Schéma SQL appliqué (supabase/migrations/20260904000000_initial_schema.sql) :
   - profiles (auto-créé à l'inscription via trigger on_auth_user_created,
     crédits initiaux = 30)
   - games, chapters, credits_transactions, purchases
   - RLS activée sur toutes les tables (propriétaire uniquement)
   - Application via `supabase db push --db-url` (pooler aws-1-eu-west-1 ;
     NOTA : le hostname du pooler était en aws-1- et non aws-0-, et la
     connexion directe db.<ref> est IPv6-only).
4. Côté app (apps/mobile) :
   - Dépendances ajoutées : @supabase/supabase-js, expo-secure-store,
     expo-auth-session, expo-web-browser, expo-crypto,
     @react-native-async-storage/async-storage
   - src/lib/supabase.ts : client configuré (SecureStore natif,
     localStorage web, session persistée + refresh auto)
   - src/services/auth.ts : signInWithEmail, signUpWithEmail,
     signInWithGoogle (OAuth PKCE via expo-web-browser), signOut,
     getCurrentSession, onAuthStateChange, exchangeCodeForSession
   - app/auth.tsx réécrit : vrais appels Supabase, messages d'erreur
     traduits, confirmation d'email gérée, bouton Google
   - app/auth/callback.tsx : route de callback OAuth (deep link)
   - app/index.tsx : vérifie la vraie session au lancement + écoute
     les changements d'état (onAuthStateChange)
   - app/(tabs)/profile.tsx : vraie déconnexion (signOut)
   - app.json : plugins expo-secure-store + expo-web-browser ajoutés
5. Côté API (api/) :
   - lib/auth.ts : getDb (service role, jamais exposé), requireUserId
     (valide le JWT via supabase.auth.getUser)
   - api/user/profile.ts : GET/PUT profil authentifié (credits, premium,
     age, préférences) - protéger par Bearer token
6. Test bout en bout : inscription via l'API auth -> utilisateur créé,
   profil auto-généré (credits=30) vérifié en base, puis nettoyé.
7. Build web + typecheck OK (mobile + API).
8. Commit + push : "Auth réelle Supabase : email/password + Google,
   session sécurisée, routes protégées, API JWT + user/profile" (58aea73)

===============================================================
SESSION 10 bis - 04/09/2026 : FIX API VERCEL (Build Output API)
===============================================================
PROBLÈME : /api/* renvoyait 404. Cause : vercel.json racine en build
statique (outputDirectory = apps/mobile/dist) -> Vercel déployait
uniquement le site, les serverless functions de api/ étaient ignorées.

SOLUTION : Build Output API (vercel.com/docs/build-output-api) :
1. scripts/build-vercel.mjs : produit .vercel/output/ avec
   - static/ = export web Expo (apps/mobile/dist)
   - functions/api/<route>.func/ = chaque api/api/**/*.ts bundlé
     (esbuild, node22, CJS) + .vc-config.json
   - config.json : filesystem + routes API + fallback SPA index.html
2. vercel.json racine : buildCommand = node scripts/build-vercel.mjs,
   outputDirectory = .vercel/output
3. esbuild ajouté en devDependency (racine).
4. Build local validé : 2 functions générées (health, user/profile),
   site exporté, config.json correct. .vercel/ reste ignoré (git).

URLS FINALES :
- Site : https://fable-app-three.vercel.app (HTTP 200)
- API : https://fable-app-three.vercel.app/api/health (à re-vérifier après deploy)
- Repo : https://github.com/HyddenTroll/fable-app
- Local : http://localhost:8081 (npm start dans apps/mobile)

===============================================================
SESSION 11 - 04/09/2026 : API JEU RÉELLE (fin du mock côté serveur)
===============================================================
CONTEXTE : analyse externe du projet -> l'essentiel manquant était
la génération IA + quota serveur + coût réel. Ticket n°1 exécuté.

FAIT :
1. Fournisseur confirmé (docs/2-analyse-strategique/choix-fournisseur-ia.txt)
   : GPT-5.6 Luna (0,20/1,20 $) pour chapitres/résumés ; Sonnet 5
   (qualité FR) pour bible/prologue/fins si clé Anthropic dispo.
2. api/lib/llm/provider.ts réécrit : streaming natif (SSE), coûts
   calculés (PRICES par modèle), prompt caching (automatique OpenAI),
   max_completion_tokens (GPT-5.6 - PAS max_tokens), response_format
   json_object CONDITIONNEL (le prompt doit contenir "JSON").
3. api/lib/prompts.ts : prompts complets (bible, prologue, chapitre,
   choix, résumé) basés sur docs/6-avant-le-code/prompts-ia.txt +
   règles anti-AI-slop + adaptation à l'âge.
4. api/lib/quota.ts : QUOTA SERVEUR dur - 5 chapitres gratuits (toutes
   parties), plafond mensuel Fable+ (200/mois), compteurs en base.
5. api/lib/cost.ts : journal cost_logs (chaque appel : tokens + coût $).
6. Endpoints :
   - POST /api/game/create : story bible + prologue (gpt-5.6-luna),
     stocke la partie, consomme 1 chapitre gratuit. Retourne gameId,
     chapitre, coverPrompt, freeChaptersRemaining.
   - POST /api/game/chapter : STREAMING SSE (event: text, event: done),
     contexte = bible + résumé glissant + N=3 derniers chapitres
     (jamais l'historique complet, évite O(n^2)), puis 2 petits appels
     (choix + résumé). Vérifie le quota AVANT génération. Log coûts.
   - GET /api/game/read?gameId= : état complet d'une partie.
7. Migration SQL 0002 : profiles (quota mensuel), cost_logs (RLS),
   vue credit_balances (solde = somme des transactions).
8. Bugfix notable : handlers API en interface HTTP native (res.statusCode,
   res.end) - les res.status().json() Express crashaient sur Vercel.
9. TEST RÉEL end-to-end (compte OpenAI rechargé) :
   - create : partie "La Cendre et la Couronne" créée, prologue généré.
   - chapter : chapitre 1 streamé (117KB), 3 choix, résumé glissant.
   - paywall : à 5 chapitres gratuits consommés -> HTTP 402 paywall:true.
   - COÛTS RÉELS (gpt-5.6-luna) :
       story_bible 0,0028 $ | prologue 0,0035 $ | chapitre 0,0047 $
       choix 0,0017 $ | résumé 0,0014 $
       => ~0,014 $ pour bible+prologue+ch.1. Rentable vs 3,5 $/mois.
10. Env : OPENAI_API_KEY ajoutée au .env local (jamais sur GitHub).
11. Deploy prod + commit/push.

RESTE À FAIRE :
- Brancher l'app mobile sur ces endpoints (fin du mock côté client).
- Clé Anthropic (facultatif) pour bible/prologue en Sonnet 5.
- Vérifier la licence commerciale FLUX.1 via fal.ai (images).
- Modération contenu (bloquant App Store) - pas une phase 2.
- Apple Sign-In (obligatoire App Store).
- Analyser coût/qualité après 20 chapitres réels (plan de test de
  choix-fournisseur-ia.txt).

===============================================================
SESSION 12 - 04/09/2026 : ÉTAT STRUCTURÉ + TEST COHÉRENCE RÉEL
===============================================================
CONTEXTE : analyse externe -> le résumé glissant en prose oublie les
faits. Correctif = état structuré déterministe (jamais de prompt
engineering). Test réel 3 runs x 20 chapitres prévu.

FAIT :
1. État structuré (api/lib/state.ts) : blessures, inventaire, pnj,
   engagements, lieu. Le modèle renvoie des DELTAS, le code applique
   le patch (applyStateDelta). Rien ne s'efface sans instruction
   explicite. Plafonds (STATE_LIMITS : 10 blessures, 20 objets,
   25 PNJ, 10 engagements) pour borner le coût de l'appel d'état.
2. Migration 0004 : games.bible_text (JSON figé verbatim) +
   games.state (jsonb). bible_text = préfixe stable pour le cache.
3. buildChapterMessages : prompt du chapitre SPLITTÉ en 2 messages
   (bible stable / contexte volatile) - structure correcte pour le
   cache futur.
4. Script test-coherence.mjs v2 : instrumente PAR APPEL (chapter /
   choices / summary / state séparés : tokens in/cachés, out, coût,
   latence). Faits replacés pour séparer âge vs type (blessure ch.12,
   PNJ ch.2...).

RÉSULTATS RÉELS (gpt-5.6-luna, runs sur l'API) :
- Coût MOYEN par chapitre : 0,0087 $ (chapitre+choix+résumé+état).
  Détail ch.1 : chapitre 0,0039 + résumé 0,0011 + état 0,0008.
  200 chapitres extrapolés : ~1,74 $ (sans cache).
- Coût run 16 ch : ~0,14 $ ; run 8 ch : ~0,07 $. TOTAL dépensé pour
  valider : ~0,21 $ (excellent rapport pour une hypothèse structurale).
- L'ÉTAT STRUCTURÉ préserve les faits : Marrek (ch.2), Elena (ch.3),
  blessures ch.1-3 tous présents dans l'état final. ✔ Par construction
  (delta + patch), pas seulement par observation.
- Le coût a passé de 0,0073 (v1, sans état) à 0,0087 $ (+19 %) avec
  l'appel d'état : attendu, acceptable, à surveiller (plafonné).

CONCLUSIONS / TICKETS (validées par l'utilisateur) :
1. CACHE GPT-5.6 : NE PAS refactorer maintenant. Le gain serait
   ~0,0008 $/chapitre (~9 % du coût, l'input ne pèse que 30 %) =
   0,05 $/abonné/mois contre ~3,50 € de marge -> BRUIT. Le chat
   completions ne supporte PAS prompt_cache_options (Responses API
   seulement) -> refactor de couche API non justifié sur un produit
   dont l'app est encore en mock. TICKET à rouvrir quand il y aura du
   volume réel. AVANT tout refactor : vérifier les noms exacts des
   paramètres (prompt_cache_options, prompt_cache_breakpoint,
   prompt_cache_key) dans la doc officielle OpenAI - ne pas se fier
   à un résumé.
2. TEST COHÉRENCE NON CONCLUANT : les runs faisaient 16 et 8 chapitres,
   la v1 échouait à 20. Séparation âge vs type non reproduite (blessure
   tardive ch.12 + PNJ précoce ch.2). À refaire UNE FOIS, plus tard,
   en régression (pas maintenant : l'état structuré est bon par
   construction).
3. PLafond Fable+ : 60/mois validé (coût ~0,52 $/mois à 0,0087 $/ch).

URLS FINALES :
- Site : https://fable-app-three.vercel.app
- API : /api/health, /api/game/create, /api/game/chapter, /api/game/read
- Repo : https://github.com/HyddenTroll/fable-app

===============================================================
SESSION 13 - 04/09/2026 : APP BRANCHÉE SUR L'API RÉELLE
===============================================================
CONTEXTE : l'app mobile tournait en mock. Branchement sur les
endpoints réels. 4 décisions produit tranchées AVANT le code :
1. Streaming : expo/fetch (ReadableStream natif) sur mobile, fetch
   standard sur web. Vérifié dans le SDK (FetchResponse.body =
   ReadableStream<Uint8Array>).
2. Échec réseau à 70 % : JETER le partiel + revenir au choix
   précédent avec bouton "Réessayer". (Le serveur ne décompte rien,
   le partiel n'est pas acquis.)
3. État structuré : le SERVEUR possède, le store AFFICHE (heroState
   dans Zustand, jamais fusionné/recalculé comme source de vérité).
4. Paywall : au CLIC seulement (pas de compteur affiché).

FAIT :
1. src/services/api.ts : client API réel. createGame, readGame,
   streamChapter (parseur SSE sur ReadableStream). Gère 402 ->
   ApiError.paywall.
2. new-game.tsx : branche sur createGame (bible+prologue réels),
   état de chargement, erreurs.
3. game/[gameId].tsx : branche sur streamChapter (machine à écrire
   réelle), affiche l'état structuré (blessures/inventaire/PNJ) reçu
   du serveur, erreur réseau -> jeter + réessayer, 402 -> /paywall.
4. store.ts : ajout heroState (affiché, pas source de vérité).
5. BUGS API corrigés au passage :
   - Bible/prologue TRONQUÉS à 3000 tokens (JSON coupé -> 502).
     maxTokens 6000 pour generateJson. Corrigé et testé.
   - Migration 0004 (bible_text, state) N'ÉTAIT PAS APPLIQUÉE ->
     "Impossible de créer la partie". Appliquée via supabase db push.
6. TEST FLUX COMPLET RÉEL (validé) :
   - create : partie "Le Dernier Nom du Soleil", freeRemaining 4
   - chapter : 2968 événements text SSE, chapitre 1 (11,8k chars),
     3 choix, état (blessure paume + PNJ Liora/Ysilde/Maëlys), coût
     0,0089 $, freeRemaining 3.
   - Le format SSE correspond exactement au parseur client.

   ================================================================
   SESSION 14 - 04/09/2026 : RELANCE SUR CE PC + TEST LIVRE + FIX ACCUEIL
   ================================================================
   CONTEXTE : reprise du travail depuis ce PC (D:\Fable resté à la
   session 9 bis). GitHub = source de vérité. L'utilisateur teste le
   site déployé et signale des problèmes. Objectif : tester un livre
   de 4 chapitres lisible + nettoyer l'accueil.

   FAIT :
   1. SYNCHRO : git pull de D:\Fable (était à 5780547, session 9 bis)
      -> ea16ecd (session 13). npm install (deps manquantes après le
      pull : typecheck mobile cassé sur expo-secure-store/expo-web-browser).
   2. RÉCUPÉRATION DES CLÉS : vercel env pull (projet lié fable-app,
      production). LESSON : les variables "Encrypted" du dashboard
      (OPENAI_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY) sont
      PULLÉES VIDES ("" - hex 2222) par le CLI. Seules les variables
      publiques EXPO_PUBLIC_* (anon key + URL) sont récupérables.
      => La clé OpenAI ne s'obtient pas via Vercel CLI sur ce PC.
   3. SCRIPT LOCAL (scripts/book-test.mjs) : pipeline complet hors-ligne
      (bible + prologue + 4 chapitres + choix + résumé + état + fin +
      HTML au thème Fable). Bloqué : pas de OPENAI_API_KEY locale.
   4. SCRIPT VIA API DÉPLOYÉE (scripts/book-via-api.mjs) : compte
      Supabase jetable (signup) puis POST /api/game/create +
      4x /api/game/chapter (SSE) sur https://fable-app-three.vercel.app,
      HTML du livre à la fin. BLOQUÉ par Supabase :
      - email jetable example.com REJETÉ (email_address_invalid)
      - gmail.com jetable -> 429 over_email_send_rate_limit
        (mailer_autoconfirm=false => chaque signup envoie un email ;
        quota emails du projet épuisé par les tests des sessions 10-13)
      => ABANDON : l'utilisateur testera via le site déployé.
      NB crash Windows : Node 24 + fetch => "Assertion failed
      uv_handle_closing" à la sortie du process (cosmétique, après le
      résultat).
   5. BUG SIGNALÉ PAR L'UTILISATEUR (accueil) : l'écran d'accueil
      affichait la boîte "Prototype - debug / Données fictives (pas
      d'IA réelle)" - périmée depuis la session 13 ET moche.
      => ACCUEIL NETTOYÉ : basiques fonctionnels seulement (Continuer
      l'aventure si partie en cours / + Nouvelle aventure / badges
      Connexion-Profil / bonjour). Commit 32e9fa9 + push (site
      redéployé automatiquement).
   6. BUG GOOGLE OAUTH (bloquant, signalé par l'utilisateur) : après
      le flux Google, Supabase redirige vers http://localhost:3000/
      #access_token=... au lieu du site Vercel.
      CAUSE : PAS un bug du code (l'app envoie bien window.location.origin
      via supabase.auth.signInWithOAuth, detectSessionInUrl=true sur
      web). CAUSE = config dashboard Supabase : Site URL encore à
      http://localhost:3000 (réglage de dev) et Redirect URLs sans le
      domaine Vercel. GoTrue retombe sur la Site URL par défaut quand
      redirect_to n'est pas autorisé.
      ACTION UTILISATEUR (dashboard supabase.com > projet fable >
      Authentication > URL Configuration) :
      - Site URL : https://fable-app-three.vercel.app
      - Redirect URLs : + https://fable-app-three.vercel.app
        et https://fable-app-three.vercel.app/auth/callback
        (garder fable://auth/callback pour le natif).
   7. LESSON GITIGNORE : .env.* ne matche pas .env2 -> le pull Vercel
      (.env2) a failli être commité (27 variables, secrets vides).
      Retiré du suivi + .gitignore élargi à .env* (avec
      !.env.example). Aucun secret réel exposé (vérifié : longueurs
      des valeurs dans le commit).
   8. Le user a collé une URL de callback contenant un refresh_token :
      recommandé de se déconnecter pour révoquer la session.
   9. SUITE (même session) : l'utilisateur a modifié les Redirect URLs
      Supabase DANS LA FOULÉE -> CONNEXION GOOGLE VALIDÉE sur le site
      déployé. Le flux complet sera testé : Nouvelle aventure ->
      createGame (bible+prologue) -> écran de lecture (prologue).

   URLS FINALES :
   - Site : https://fable-app-three.vercel.app
   - Repo : https://github.com/HyddenTroll/fable-app
   - Scripts de test livre : scripts/book-test.mjs (local, besoin clé)
     et scripts/book-via-api.mjs (via API déployée, besoin quota email)

   ================================================================
   SESSION 15 - 04/09/2026 : V2 NARRATION (CAP PRÉCIS + MÉMOIRE DU PLAN + PROSE)
   ================================================================
   CONTEXTE : retours utilisateur sur la qualité. 3 problèmes :
   1. L'IA n'a pas d'idée précise d'où elle emmène le lecteur.
   2. Un choix important ne fait pas reconsidérer l'histoire (elle
      dérive ou ignore la conséquence). Il faut réécrire les grandes
      lignes EN MÉMOIRE avant de continuer.
   3. Le prologue généré fait "amateur" : style haché (phrases
      nominales, paragraphes d'une phrase, "Elle fit... Elle vit...").

   FAIT (archi v2 : le cap est figé, la route s'adapte) :
   1. TYPES (packages/shared/src/types.ts) :
      - StoryBible.planDirecteur (OBLIGATOIRE pour les nouvelles
        parties ; les vieilles bibles n'en ont pas -> géré en code).
      - PlanDirecteur : destination (LE cap), noyauImmuable (3-5
        vérités qui ne changent jamais), actes (objectif + scènes
        clés ordonnées + tournant de fin d'acte), carrefours (2-5
        choix majeurs anticipés + conséquence si déviation), fins
        (condition précise + émotion finale).
      - StoryPlan (mémoire évolutive) : grandesLignes (150-250 mots),
        derniereMiseAJourChapitre, versQuelleFin.
   2. PROMPTS (api/lib/prompts.ts) :
      - buildStoryBiblePrompt : exige planDirecteur complet (le
        modèle DOIT planifier avant d'écrire).
      - PROSE_RULES (nouveau) : prose de roman publié, interdit le
        style haché, scènes = 2-4 par chapitre, paragraphes amples
        4-8 phrases, rythme varié, descriptions concrètes, point de
        vue strict.
      - buildProloguePrompt : 1500-2200 mots (était 800-1200) +
        PROSE_RULES + "faire sentir le cap sans le révéler".
      - buildChapterMessages : paramètre plan (grandes lignes)
        injecté dans le volatile ; longueurs chapitres 2000/3500/5000
        (était 1500/3000/5000) ; règles renforcées (faire AVANCER le
        cap, pas prolonger).
      - buildPlanReconsiderPrompt (nouveau) : juge si le choix du
        lecteur est un TOURNANT - si oui, réécrit les grandes lignes
        (la route s'adapte, plan directeur + noyau immuable restent).
   3. PROVIDER (api/lib/llm/provider.ts) : kind 'plan' ajouté
      (gpt-5.6-luna).
   4. ENDPOINT chapter (api/api/game/chapter.ts) :
      - Injecte story_plan.grandesLignes dans le prompt du chapitre.
      - Après le choix du lecteur (playerChoiceLabel) : appelle la
        réévaluation du plan SEULEMENT si la colonne story_plan
        existe en base (auto-détection -> l'API reste stable avant
        migration). Si important : stocke le nouveau plan dans
        games.story_plan + log cost kind 'plan'.
      - Coût ajouté : ~0,001 $/chapitre max (appel court, seulement
        quand il y a un choix + plan directeur).
   5. MIGRATION 20260904040000_story_plan.sql :
      - games.story_plan jsonb (NULL au départ).
      - cost_logs : contrainte kind étendue avec 'plan'.
      - À APPLIQUER par l'utilisateur (pas de CLI supabase ni db-url
        sur ce PC) : dashboard supabase.com > SQL editor > coller le
        SQL du fichier. SANS cette migration : le jeu fonctionne
        comme avant (pas de mémoire du plan - la bible reste la
        référence, le planDirecteur est dans les prompts).
   6. Typecheck : api + shared + mobile OK. Commit + push
      (déploiement auto Vercel).

   COÛT IMPACTÉ (estimé) : chapitre ~0,0087 $ -> ~0,0097 $ avec
   l'appel plan (seulement quand choix + colonne + planDirecteur).

   7. BUG SIGNALÉ (même session) : l'IA sortait DEUX FOIS LA MÊME
      histoire quand on créait 2 parties avec les mêmes paramètres.
      CAUSE : le prompt de bible était 100 % identique (le nom du
      héros n'était pas injecté) et GPT-5.6 ré-échantillonne la même
      histoire sans stimulation de variété. Le caching n'est PAS en
      cause (il n'affecte que les tokens d'entrée).
      FIX : buildStoryBiblePrompt reçoit heroName/heroTrait (créé par
      l'utilisateur) + "INDICE DE CRÉATION" (numéro de tirage
      aléatoire) + consigne d'originalité ; temperature 1.1 sur
      bible/prologue, 1.0 sur le chapitre ; LLMRequest.temperature
      ajouté au provider (OpenAI + Anthropic).
      Effet attendu : chaque nouvelle histoire est différente dès le
      départ, même avec les mêmes réglages.
      NB ERREUR PROD (signalée par l'utilisateur) : "400 Unsupported
      value: 'temperature' does not support 1.1 with this model.
      Only the default (1) value is supported." -> GPT-5.6 Luna
      n'accepte QUE temperature=1 (défaut). La température 1.1/1.0
      a été RETIRÉE des appels (create + chapter). LESSON : sur ce
      modèle, la variété se joue au niveau du PROMPT (nonce, nom du
      héros), jamais via temperature. Le champ temperature du
      provider reste pour Anthropic (Sonnet supporte 0-1).

   ================================================================
   PROCHAINE SESSION (à faire)
   ================================================================
   1. [FAIT - user] Dashboard Supabase : Redirect URLs ajoutées
      (domaine Vercel) - confirmé par l'utilisateur dans la foulée.
      Site URL à vérifier si le flux Google reste bancal.
   2. [DÈS QUE POSSIBLE] Test livre 4 chapitres : relancer
      scripts/book-via-api.mjs une fois le rate limit email écoulé
      (~1h) ou avec un compte user existant. Lire le HTML produit.
   3. [TICKET CACHÉ] Rouvrir quand volume réel : cache explicite GPT-5.6
      via Responses API (vérifier d'abord la doc officielle)
   4. [RÉGRESSION] Test cohérence complet 20 chapitres + séparation
      âge/type
   5. Re-test sur téléphone (Expo Go) - le streaming expo/fetch doit être
      validé en conditions réelles
   6. Continuer une partie au retour (actuellement le store garde le jeu
      courant en session, mais pas de rechargement via /api/game/read
      après redémarrage)
   7. Clé OpenAI sur ce PC : à recopier depuis l'autre PC (le .env n'est
      PAS sur GitHub ni récupérable via vercel env pull).
   8. [MIGRATION à appliquer] SQL de 20260904040000_story_plan.sql
      dans le dashboard Supabase (SQL editor) pour activer la mémoire
      du plan (story_plan + kind 'plan'). Sans elle : fonctionne
      sans plan évolutif.
   9. [FAIT - user] Migration 0005 APPLIQUÉE via le SQL editor
      (confirmé : "Success. No rows returned") -> story_plan jsonb
      sur games ; contrainte cost_logs_kind_check supprimée. La
      mémoire du plan est active. À confirmer : ré-ajout de la
      contrainte avec 'plan' (3e instruction du bloc).